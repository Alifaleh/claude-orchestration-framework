# Workspace repo — commits Claude material and team memory (CLAUDE.md, .claude/**, ONBOARDING.md,
# workspace.yaml, example.env). Everything below is machine-local or lives in its own repo.
.env
tmp/
graphify-out/
.claude/settings.local.json
.claude/last-pulse-nudge

# Code repos (each its own git repo — registered in workspace.yaml; one line per repo path):
# /backend/
# /frontend/
